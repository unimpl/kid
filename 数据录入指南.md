# 数据规格与书籍扫描流程

知识图谱的数据已从 `index.html` 中独立出来，存为带 JSON Schema 约定的纯数据文件。
扫描一本新书时，只需让它产出**符合下述规格**的数据，即可并入图谱渲染。

## 目录结构

```
data/
  subjects.json        学科定义（key / 名称 / 配色）
  concepts.json        概念节点（图谱的「点」）
  dependencies.json    前置依赖（图谱的「边」）
  manifest.json        计数、年龄范围、SHA-256 校验和（由构建脚本生成）
schema/
  subjects.schema.json       学科 JSON Schema
  concepts.schema.json       概念 JSON Schema
  dependencies.schema.json   依赖 JSON Schema
scripts/
  validate.py          完整性校验（结构 / 引用 / 去重 / 无环）
  build_bundle.py      生成 manifest.json 与 dist/data.js
dist/
  data.js              渲染用打包文件（window.KID_DATA），由构建脚本生成
index.html             渲染器，通过 <script src="dist/data.js"> 加载数据
```

## 数据模型

### 学科 `data/subjects.json`

```json
{ "key": "math", "name": "数学", "color": "#3D63D8" }
```

- `key` — 稳定标识（小写字母/数字/下划线），概念的 `subject` 字段引用它。
- `name` — 显示名。`color` — `#RRGGBB` 配色。

### 概念 `data/concepts.json`

```json
{
  "id": "m_count20",
  "subject": "math",
  "grade": 1,
  "domain": "数与代数",
  "title": "数数1到20",
  "question": "你的孩子能接着10往下数到20，并认出每个数字吗？",
  "source": "人教数学一上"
}
```

- `id` — 全局唯一，小写字母/数字/下划线，被依赖引用。建议 `<学科前缀>_<含义>`。
- `subject` — 必须是 `subjects.json` 里存在的 `key`。
- `grade` — 年级：0=学前，1-6=小学1-6年级。
- `domain` — 所属领域（如「数与代数」「拼音与识字」）。
- `title` — 概念标题。`question` — 家长判断孩子是否掌握的引导问。
- `source` —（可选）来源教材标识，扫描书时填写，便于溯源。

### 依赖 `data/dependencies.json`

```json
{ "topicId": "m_count20", "prerequisiteId": "m_count10", "strength": "hard", "reason": "先会数10以内，再扩展到20" }
```

- `topicId` **依赖于** `prerequisiteId`（学会前者需先掌握后者）。反向即「解锁」。
- `strength` — `hard`（必须先学）/ `soft`（建议先学）。
- `reason` —（可选）一句话说明。
- 两端必须指向 `concepts.json` 中已存在的 `id`；不允许自依赖、重复边或成环。

## 扫描一本书的流程

1. **翻书，提炼概念**：把书中每个可教学的知识点写成一条 `concepts.json` 记录，
   `subject` 填对应学科 key，`grade` 填年级（0=学前，1-6=小学），`source` 填这本书的标识。
2. **连前置关系**：对每个概念，找出「学会它之前必须先会的」概念，在
   `dependencies.json` 加一条 `topicId → prerequisiteId` 的边。跨书的引用同样有效，
   只要对方 `id` 已存在。
3. **校验**：`python3 scripts/validate.py` —— 确认 id 唯一、学科合法、依赖端点可解析、无环。
4. **构建打包**：`python3 scripts/build_bundle.py` —— 重新生成 `manifest.json` 与 `dist/data.js`。
5. **渲染**：直接用浏览器打开 `index.html`（无需服务器，`data.js` 经 `<script>` 加载，兼容 `file://`）。

## 常用命令

```bash
python3 scripts/validate.py      # 校验数据完整性
python3 scripts/build_bundle.py  # 重新生成 manifest + dist/data.js
```

> 规格即契约：只要扫描产出的概念与依赖能通过 `validate.py`，就能被 `index.html` 正确渲染。
